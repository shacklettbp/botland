set(BOT_OS_EMSCRIPTEN OFF)
set(BOT_OS_ANDROID OFF)
set(BOT_OS_LINUX OFF)
set(BOT_OS_MACOS OFF)
set(BOT_OS_IOS OFF)
set(BOT_OS_WINDOWS OFF)

set(BOT_ARCH_X64 OFF)
set(BOT_ARCH_ARM OFF)

if (EMSCRIPTEN)
  set(BOT_OS_EMSCRIPTEN ON)
elseif (ANDROID)
  set(BOT_OS_ANDROID ON)
elseif (CMAKE_SYSTEM_NAME STREQUAL "Linux")
  set(BOT_OS_LINUX ON)
elseif (IOS)
  set(BOT_OS_IOS ON)
elseif (APPLE)
  set(BOT_OS_MACOS ON)
elseif (CMAKE_SYSTEM_NAME STREQUAL "Windows")
  set(BOT_OS_WINDOWS ON)
endif ()

if (NOT BOT_OS_EMSCRIPTEN)
  if (CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64")
    set(BOT_ARCH_X64 ON)
  elseif (CMAKE_SYSTEM_PROCESSOR STREQUAL "arm")
    set(BOT_ARCH_ARM ON)
  endif ()
endif()
